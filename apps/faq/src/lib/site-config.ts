import { createSiteIcon } from '@windrun-huaiin/base-ui/components/server';
import { ChevronsLeft, ChevronsRight, Locate } from 'lucide-react';

export const SiteIcon = createSiteIcon('Usb');

export const LocateIcon = createSiteIcon(Locate);
export const ChevronsLeftIcon = createSiteIcon(ChevronsLeft);
export const ChevronsRightIcon = createSiteIcon(ChevronsRight);
